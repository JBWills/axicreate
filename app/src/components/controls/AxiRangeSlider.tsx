import BaseSlider, { RangeProps } from "./BaseSlider";

const AxiRangeSlider = (props: RangeProps) => {
  // eslint-disable-next-line react/jsx-props-no-spreading
  return <BaseSlider {...props} />;
};

export default AxiRangeSlider;
