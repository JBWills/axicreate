import BaseSlider, { SliderProps } from "./BaseSlider";

const AxiSlider = (props: SliderProps) => {
  // eslint-disable-next-line react/jsx-props-no-spreading
  return <BaseSlider {...props} />;
};

export default AxiSlider;
