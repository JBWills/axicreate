import { V2 } from "./Vec";

type AppWindowPlacement = {
  windowSize: V2;
  windowOffset: V2;
  controlDrawerPercent: number;
  previewScale: number;
  previewOffset: V2;
};

export default AppWindowPlacement;
